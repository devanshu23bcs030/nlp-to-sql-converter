# 🧠 NLP to SQL Query Converter

An intelligent Natural Language Processing (NLP) based system that converts human language queries into SQL statements and provides an interactive UI to visualize and explore database results.

---

## 🚀 Project Overview

This project allows users to input natural language queries such as:

> "Show all customers from Delhi"
>  
> "Find products with price greater than 500"

The system processes the input using NLP techniques, converts it into a valid SQL query, executes it on the database, and displays the results in a clean and user-friendly interface.

---

## ✨ Features

- 🔎 Convert Natural Language to SQL queries
- 🗄️ Execute generated SQL on a database
- 📊 Interactive UI to display database tables
- ⚡ Real-time query processing
- 🧾 SQL query preview before execution
- 📁 Database file visualization

---

## 🛠️ Tech Stack

- Python (Backend Logic)
- NLP Libraries (e.g., NLTK / spaCy / Transformers)
- SQLite / MySQL (Database)
- Flask / FastAPI (Backend Framework)
- HTML, CSS, JavaScript (Frontend UI)

---

## 🧩 System Architecture

1. User enters a natural language query
2. NLP model processes and extracts intent
3. Query is converted into SQL syntax
4. SQL query is executed on the database
5. Results are displayed in the UI

